global:
  resolve_timeout: 5m

route:
  group_by: [alertname, env, instance]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: discord

receivers:
  - name: discord
    webhook_configs:
      - url: http://discord-alerts:9094
        send_resolved: true
