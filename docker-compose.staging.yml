services:
  api:
    image: ghcr.io/mondony/yadony-back:${YADONY_IMAGE_TAG:-staging}
    container_name: yadony_api
    restart: unless-stopped
    # Secrets provisionnés via .env.staging (déposé par le workflow de déploiement) :
    # une variable ajoutée là-bas atteint le conteneur sans toucher au compose.
    env_file:
      - .env.staging
    environment:
      SPRING_PROFILES_ACTIVE: staging
      GOOGLE_APPLICATION_CREDENTIALS: /app/firebase-service-account.json
      APP_BASE_URL: https://api-staging.yadony.com
      SPRING_DATASOURCE_URL: jdbc:postgresql://yadony_db_staging:5432/yadony_staging
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      SPRING_CONFIG_ADDITIONAL_LOCATION: file:/app/config/
    volumes:
      - ./firebase-service-account.json:/app/firebase-service-account.json:ro
      - ./application-staging.yml:/app/config/application-staging.yml:ro
    depends_on:
      db:
        condition: service_healthy
    networks:
      - yadony_internal
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/api/v1/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  alloy:
    image: grafana/alloy:v1.8.3
    container_name: yadony_alloy
    restart: unless-stopped
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
    volumes:
      - ./monitoring/alloy/config.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - yadony_alloy_data:/var/lib/alloy/data
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host/root:ro,rslave
    networks:
      - yadony_internal
    depends_on:
      - api

  db:
    image: postgres:16-alpine
    container_name: yadony_db_staging
    restart: unless-stopped
    environment:
      POSTGRES_DB: yadony_staging
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - yadony_db_staging_data:/var/lib/postgresql/data
    networks:
      - yadony_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d yadony_staging"]
      interval: 10s
      timeout: 5s
      retries: 10

  db-backup:
    image: postgres:16-alpine
    container_name: yadony_db_backup_staging
    restart: unless-stopped
    environment:
      PGPASSWORD: ${DB_PASSWORD}
    volumes:
      - ./backups:/backups
    networks:
      - yadony_internal
    # Backup immédiat au démarrage, puis toutes les 24h. Keep last 7 days.
    entrypoint: >
      sh -c "
        backup() {
          FILE=/backups/backup_$$(date +%Y%m%d_%H%M%S).sql.gz;
          pg_dump -h db -U ${DB_USERNAME} yadony_staging | gzip > $$FILE;
          echo \"Backup created: $$FILE\";
          find /backups -name 'backup_*.sql.gz' -mtime +7 -delete;
        };
        backup;
        while true; do sleep 86400 & wait $$!; backup; done
      "
    depends_on:
      db:
        condition: service_healthy

  certbot:
    image: certbot/certbot:latest
    container_name: yadony_certbot
    restart: unless-stopped
    volumes:
      - ./nginx/certs:/etc/letsencrypt
      - ./nginx/www:/var/www/certbot
    entrypoint: >
      sh -c "
        certbot certonly --webroot -w /var/www/certbot -d api-staging.yadony.com --non-interactive --agree-tos --email ops@yadony.com --keep-until-expiring --quiet;
        while true; do
          sleep 86400 &
          wait $$!
          certbot renew --webroot -w /var/www/certbot --quiet
        done
      "
    depends_on:
      - nginx

  nginx:
    image: nginx:1.27-alpine
    container_name: yadony_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.staging.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/letsencrypt:ro
      - ./nginx/static:/var/www/static:ro
      - ./nginx/www:/var/www/certbot:ro
    depends_on:
      - api
    networks:
      - yadony_internal

  stripe-cli-payments:
    image: stripe/stripe-cli:latest
    container_name: yadony_stripe_cli_payments
    restart: unless-stopped
    environment:
      STRIPE_API_KEY: ${STRIPE_SECRET_KEY}
    command: >
      listen
      --api-key ${STRIPE_SECRET_KEY}
      --device-name staging-payments
      --forward-to http://api:8080/api/v1/payments/webhook
      --events charge.captured,charge.failed,charge.refunded,payment_intent.succeeded,payment_intent.payment_failed,payment_intent.canceled
    depends_on:
      api:
        condition: service_healthy
    networks:
      - yadony_internal

  stripe-cli-kyc:
    image: stripe/stripe-cli:latest
    container_name: yadony_stripe_cli_kyc
    restart: unless-stopped
    environment:
      STRIPE_API_KEY: ${STRIPE_SECRET_KEY}
    command: >
      listen
      --api-key ${STRIPE_SECRET_KEY}
      --device-name staging-kyc
      --forward-to http://api:8080/api/v1/kyc/webhook
      --events identity.verification_session.verified,identity.verification_session.requires_input
    depends_on:
      api:
        condition: service_healthy
    networks:
      - yadony_internal

  prometheus:
    image: prom/prometheus:latest
    container_name: yadony_prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-remote-write-receiver'
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - yadony_prometheus_data:/prometheus
    networks:
      - yadony_internal
    ports:
      - "9090:9090"
    depends_on:
      - api

  grafana:
    image: grafana/grafana:latest
    container_name: yadony_grafana
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_ADMIN_PASSWORD:-admin}
      GF_SECURITY_ADMIN_USER: admin
      GF_SERVER_ROOT_URL: http://141.95.41.96:3000
      GF_INSTALL_PLUGINS: grafana-piechart-panel
      # Lus par les fichiers de provisioning alerting/contactpoints.yaml via $__env{}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID}
    volumes:
      - yadony_grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    networks:
      - yadony_internal
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

volumes:
  yadony_db_staging_data:
  yadony_alloy_data:
  yadony_prometheus_data:
  yadony_grafana_data:

networks:
  yadony_internal:
    driver: bridge
