<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/>
	</parent>

	<groupId>com.yadony</groupId>
	<artifactId>yadony-back</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>yadony-back</name>
	<description>yadony P2P marketplace backend</description>

	<properties>
		<!-- Tags exclus par défaut ; -DexcludedGroups= (vide) pour lancer firebase-live -->
		<excludedGroups>firebase-live</excludedGroups>
		<java.version>21</java.version>
		<jjwt.version>0.12.6</jjwt.version>
		<stripe.version>33.3.0</stripe.version>
		<firebase-admin.version>9.4.2</firebase-admin.version>
		<sentry.version>7.22.4</sentry.version>
		<aws-sdk.version>2.28.24</aws-sdk.version>
		<twilio.version>10.6.5</twilio.version>
		<springdoc.version>2.8.17</springdoc.version>
		<!-- Quality plugins -->
		<jacoco.version>0.8.12</jacoco.version>
		<spotbugs.version>4.8.6.4</spotbugs.version>
		<!-- CVE hebdo : commons-lang3 3.17.0 (géré transitivement, ex. embedded-postgres)
		     est concerné par CVE-2025-48924 (récursion incontrôlée / DoS dans ClassUtils,
		     corrigée en 3.18.0). Le parent Spring Boot 3.5.16 ne remonte pas cette version
		     -> override explicite ci-dessous. -->
		<commons-lang3.version>3.20.0</commons-lang3.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>${commons-lang3.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- Spring Boot starters -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>

		<!-- Database -->
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<!-- Cache -->
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>

		<!-- Firebase Admin SDK -->
		<dependency>
			<groupId>com.google.firebase</groupId>
			<artifactId>firebase-admin</artifactId>
			<version>${firebase-admin.version}</version>
		</dependency>
		<!-- Force google-auth-library-credentials ≥ 1.29.0:
		     google-cloud-firestore-3.28.0 (transitive via firebase-admin) requires
		     com.google.auth.ApiKeyCredentials which was not present in 1.23.0.
		     The Spring Boot BOM pins credentials to 1.23.0; this direct dep wins. -->
		<dependency>
			<groupId>com.google.auth</groupId>
			<artifactId>google-auth-library-credentials</artifactId>
			<version>1.29.0</version>
		</dependency>

		<!-- Stripe -->
		<dependency>
			<groupId>com.stripe</groupId>
			<artifactId>stripe-java</artifactId>
			<version>${stripe.version}</version>
		</dependency>

		<!-- JWT (jjwt) -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Twilio (SMS) -->
		<dependency>
			<groupId>com.twilio.sdk</groupId>
			<artifactId>twilio</artifactId>
			<version>${twilio.version}</version>
		</dependency>

		<!-- AWS SDK S3 (Hetzner Object Storage compatible) -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<version>${aws-sdk.version}</version>
		</dependency>

		<!-- Thumbnailator — image resizing + thumbnail generation -->
		<dependency>
			<groupId>net.coobird</groupId>
			<artifactId>thumbnailator</artifactId>
			<version>0.4.20</version>
		</dependency>

		<!-- ZXing — QR code generation -->
		<dependency>
			<groupId>com.google.zxing</groupId>
			<artifactId>core</artifactId>
			<version>3.5.3</version>
		</dependency>
		<dependency>
			<groupId>com.google.zxing</groupId>
			<artifactId>javase</artifactId>
			<version>3.5.3</version>
		</dependency>

		<!-- OpenAPI / Swagger UI -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<!-- Sentry -->
		<dependency>
			<groupId>io.sentry</groupId>
			<artifactId>sentry-spring-boot-starter-jakarta</artifactId>
			<version>${sentry.version}</version>
		</dependency>

		<!-- Micrometer — export des métriques au format Prometheus -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<!-- Test -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- ─── Cucumber E2E ──────────────────────────────────────────────────── -->
		<dependency>
			<groupId>io.cucumber</groupId>
			<artifactId>cucumber-java</artifactId>
			<version>7.18.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.cucumber</groupId>
			<artifactId>cucumber-spring</artifactId>
			<version>7.18.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.cucumber</groupId>
			<artifactId>cucumber-junit-platform-engine</artifactId>
			<version>7.18.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-suite</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-suite-engine</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Programmatic JUnit Platform launcher: lets CucumberE2ETest run the
		     Cucumber engine and assert on its summary, so failing scenarios FAIL
		     the Maven build (Surefire does not report tests nested under the
		     JUnit Platform Suite engine). -->
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- ─── Testcontainers ───────────────────────────────────────────────── -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- ─── Embedded PostgreSQL (E2E — avoids Docker API version issues) ── -->
		<dependency>
			<groupId>io.zonky.test</groupId>
			<artifactId>embedded-postgres</artifactId>
			<version>2.0.7</version>
			<scope>test</scope>
		</dependency>

		<!-- ─── RestAssured ─────────────────────────────────────────────────── -->
		<dependency>
			<groupId>io.rest-assured</groupId>
			<artifactId>rest-assured</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>

			<!-- OWASP dependency-check : pas d'<executions> (non lié au cycle de vie standard,
			     donc sans effet sur mvn test/verify) — sert uniquement à fournir la config
			     (suppressionFile) au job "Security Weekly" qui invoque le goal directement
			     via `mvn org.owasp:dependency-check-maven:check`. -->
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>12.2.2</version>
				<configuration>
					<suppressionFiles>
						<suppressionFile>${project.basedir}/dependency-check-suppressions.xml</suppressionFile>
					</suppressionFiles>
				</configuration>
			</plugin>

			<!-- Surefire : augmente le timeout d'arrêt du JVM forké
			     Spring Boot peut prendre >30s à se fermer → JaCoCo corrompt son exec file -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- Tests marqués @Tag("firebase-live") frappent le vrai Firebase :
					     jamais dans la suite normale ni la CI. Lancer à la demande via
					     -Dgroups=firebase-live (voir FirebaseContactLiveIT). -->
					<excludedGroups>${excludedGroups}</excludedGroups>
					<forkedProcessExitTimeoutInSeconds>120</forkedProcessExitTimeoutInSeconds>
					<!-- Force English locale so Cucumber's {double} uses '.' as decimal separator -->
					<!-- @{argLine} preserves JaCoCo's prepare-agent argLine so coverage is captured -->
					<argLine>@{argLine} -Duser.language=en -Duser.country=US</argLine>
					<environmentVariables>
						<DOCKER_HOST>unix:///var/run/docker.sock</DOCKER_HOST>
					</environmentVariables>
					<!-- Include *IT.java so MockMvc integration tests are part of the Surefire run -->
					<includes>
						<include>**/*Test.java</include>
						<include>**/*Tests.java</include>
						<include>**/Test*.java</include>
						<include>**/*IT.java</include>
					</includes>
				</configuration>
			</plugin>

			<!-- JaCoCo: couverture — prépare l'agent, génère le rapport HTML, enforces 80% -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<!-- 1. Instrument JVM before tests -->
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<!-- 2. Generate HTML report in target/site/jacoco after tests -->
					<execution>
						<id>jacoco-report</id>
						<phase>test</phase>
						<goals><goal>report</goal></goals>
					</execution>
					<!-- 3. Build fails if coverage drops below floor — floors reflect current state,
					     à remonter au fur et à mesure que la couverture progresse.
					     Couverture mesurée : instructions ~80%, branches ~66% (2026-05-18) -->
					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>INSTRUCTION</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.75</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.70</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
							<excludes>
								<!-- Main app bootstrap -->
								<exclude>com/yadony/api/YadonyBackApplication.class</exclude>
								<!-- Spring configuration beans -->
								<exclude>com/yadony/api/config/**</exclude>
								<!-- HTTP filter (requires FirebaseAuth + Spring context) -->
								<exclude>com/yadony/api/auth/FirebaseTokenFilter.class</exclude>
								<!-- Infrastructure / stub-only services -->
								<exclude>com/yadony/api/notifications/FcmService.class</exclude>
								<exclude>com/yadony/api/notifications/SmsService.class</exclude>
								<exclude>com/yadony/api/common/StorageService.class</exclude>
								<exclude>com/yadony/api/common/StorageController.class</exclude>
								<!-- Thin HTTP adapters (controllers) -->
								<exclude>com/yadony/api/**/*Controller.class</exclude>
								<!-- Cron schedulers -->
								<exclude>com/yadony/api/**/*Scheduler.class</exclude>
								<!-- Spring event listeners -->
								<exclude>com/yadony/api/**/*EventListener.class</exclude>
								<!-- JPA entities -->
								<exclude>com/yadony/api/**/*Entity.class</exclude>
								<exclude>com/yadony/api/common/BaseEntity.class</exclude>
								<!-- Spring Data repository interfaces -->
								<exclude>com/yadony/api/**/*Repository.class</exclude>
								<!-- DTO classes (pure data) -->
								<exclude>com/yadony/api/**/dto/**</exclude>
								<!-- Event POJOs -->
								<exclude>com/yadony/api/**/events/**</exclude>
								<!-- Enums -->
								<exclude>com/yadony/api/auth/KycStatus.class</exclude>
								<exclude>com/yadony/api/auth/Role.class</exclude>
								<exclude>com/yadony/api/auth/UserStatus.class</exclude>
								<exclude>com/yadony/api/matching/BidStatus.class</exclude>
								<exclude>com/yadony/api/matching/AnnouncementStatus.class</exclude>
								<exclude>com/yadony/api/kyc/KycVerificationStatus.class</exclude>
								<exclude>com/yadony/api/tracking/TrackingEventType.class</exclude>
								<exclude>com/yadony/api/payments/PaymentStatus.class</exclude>
								<!-- Thin exception wrappers and common utilities -->
								<exclude>com/yadony/api/common/YadonyBusinessException.class</exclude>
								<exclude>com/yadony/api/common/YadonyNotFoundException.class</exclude>
								<exclude>com/yadony/api/common/PageResponse.class</exclude>
								<!-- Admin module -->
								<exclude>com/yadony/api/admin/**</exclude>
								<!-- JPA Specification (lambda-only) -->
								<exclude>com/yadony/api/matching/AnnouncementSpecification.class</exclude>
								<exclude>com/yadony/api/matching/AnnouncementSpecification$*.class</exclude>
								<!-- External infrastructure service wrappers (requires live Firestore) -->
								<exclude>com/yadony/api/messaging/FirestoreService.class</exclude>
								<!-- Conversation service: tightly coupled with FirestoreService (above) -->
								<exclude>com/yadony/api/messaging/ConversationService.class</exclude>
								<!-- Analytics aggregation services (complex SQL queries — covered by E2E) -->
								<exclude>com/yadony/api/matching/ProAnalyticsService.class</exclude>
								<exclude>com/yadony/api/matching/TravelerStatsService.class</exclude>
								<!-- Stripe inbox enums (trivial generated code) -->
								<exclude>com/yadony/api/common/stripe/StripeWebhookSource.class</exclude>
								<exclude>com/yadony/api/common/stripe/StripeEventStatus.class</exclude>
								<!-- Chargeback status enum -->
								<exclude>com/yadony/api/payments/chargeback/ChargebackStatus.class</exclude>
								<!-- City data loader (bulk import — integration only) -->
								<exclude>com/yadony/api/city/GeoNamesDataLoader.class</exclude>
							</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- SpotBugs: static analysis — invoqué explicitement par CI (spotbugs:check)
			     Pas d'execution liée au cycle Maven pour ne pas bloquer ./mvnw verify -->
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<version>${spotbugs.version}</version>
				<configuration>
					<effort>Max</effort>
					<threshold>High</threshold>
					<failOnError>true</failOnError>
					<xmlOutput>true</xmlOutput>
					<excludeFilterFile>${project.basedir}/spotbugs-exclude.xml</excludeFilterFile>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
